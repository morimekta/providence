params {
    http_port = 8080
    admin_port = 8088
}

config.Service {
    http = {
        port = params.http_port
        context = "/app"
    }
    admin {
        port = params.admin_port
    }
}
