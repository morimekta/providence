config.Database {
    uri = "jdbc:h2:localhost:mem"
    driver = "org.h2.Driver"
    unknown_field {
       more_unknowns = 5528
       other = db.something {
         else = "something else"
       }
    }
}
